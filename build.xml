<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project default="build" name="betteru">
	<property file="build.properties" />
	<property file="build-user.properties" />

	<path id="classpath">
		<fileset dir="${lib.dir}" includes="**/*.jar" />
		<fileset dir="${wildfly.modules.dir}" includes="**/*.jar" />
	</path>

	<target name="clean">
		<delete dir="${build.dir}" />
	</target>

	<target name="build">
		<mkdir dir="${build.classes.dir}" />
		<javac srcdir="${src.java.dir}" destdir="${build.classes.dir}" classpathref="classpath" includeantruntime="false" />
	</target>
	
	<!-- ===================== -->
	<!-- Creating EJB Jar      -->
	<!-- ===================== -->
	<target name="build-ejb-jar" depends="build">
		<jar destfile="${build.dist.dir}/${ejb.jar}">
			<manifest file="${source.java.dir}/META-INF/MANIFEST.MF" />
			<fileset dir="${build.classes.dir}">
				<filename name="**/*.class" />
			</fileset>
		</jar>
	</target>
	

	<target name="create_jar" depends="build">
		<jar destfile="${jar.name}" filesetmanifest="mergewithoutmain">
			<manifest>
				<attribute name="Main-Class" value="${main-class}" />
				<attribute name="Class-Path" value="." />
			</manifest>
			<fileset dir="${build.dir}">
				<filename name="**/*.class" />
			</fileset>
		</jar>
	</target>

	<target name="clean_build" depends="clean,build" />

	<target name="run" depends="create_jar">
		<java jar="${jar.name}" fork="true">
			<classpath>
				<path refid="classpath" />
				<path location="${jar.name}" />
			</classpath>
		</java>
	</target>
</project>
